% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing.R
\name{set_missing}
\alias{set_missing}
\title{Declare missing values for variable type prediction}
\usage{
set_missing(all = NULL, ...)
}
\arguments{
\item{all}{A numeric vector of global missing values to be applied to all
numeric and factor variables in the dataset. Use this when the same codes
(e.g. 98, 99) represent missing data across multiple variables.}

\item{...}{Named numeric vectors specifying per-variable missing values.
Each name must correspond to a column in the data passed to
\code{\link[=predict_type]{predict_type()}}.}
}
\value{
An object of class \code{typerclass_missing}, which is a list with two
components:
\describe{
\item{all}{The global missing value vector, or \code{NULL} if not set.}
\item{per_variable}{A named list of per-variable missing value vectors.}
}
}
\description{
\code{set_missing()} creates a specification of missing values that can be passed
to \code{\link[=predict_type]{predict_type()}} via the \code{missing} argument. Values declared as missing
are converted to \code{NA} before computing the distributional metrics used for
type prediction, effectively excluding them from the analysis.
}
\examples{
# Global missing values applied to all eligible variables
set_missing(all = c(98, 99))

# Per-variable missing values
set_missing(HWACTUAL = 99, STAPRO = c(9, 99))

# Combined: global + variable-specific exceptions
set_missing(all = c(98, 99), HWACTUAL = 999)
}
