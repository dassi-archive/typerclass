
# Numeric
var_numeric      <- c(1, 2, 2, 3, 4, 4, 4)
var_numeric_with_na     <- c(1, 2, NA, 2, 3, NA)
var_numeric_all_equal   <- c(7, 7, 7, 7)
var_numeric_only_na <- c(NA_real_, NA_real_, NA_real_)

# Character
var_char_simple         <- c("a", "b", "a", "c")
var_char_with_na        <- c("x", NA, "y", "x")
var_char_empty          <- character(0)

# Logical
var_logical <- c(TRUE, TRUE, FALSE, NA)
var_na <- c(NA, NA, NA)


#TODO: in fase di ingest gestire le stringhe e vari tipi di R (es. date)perclassificare
#in modo deterministico type.

